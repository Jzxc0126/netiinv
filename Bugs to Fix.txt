-Edit button not functioning in item is in more than 10
-Proper Validation , Excessive use of alert<>
-Editing required to enter department everty time
SELECT a.itemcode,a.brand,a.itemname,
		b.category,
        c.locationname,
        d.assetstatus,
        e.unit from inventory_tbl as a INNER JOIN category_tbl as b
        on a.categoryid=b.categoryid INNER JOIN location_tbl as c
        on a.locationid=c.locationid INNER JOIN assetstatus_tbl as d
        on a.assetstatusid=d.assetstatusid INNER JOIN unit_tbl as e 
        on a.unitid=e.unitid where 1